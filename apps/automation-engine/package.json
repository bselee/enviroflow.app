{
  "name": "@enviroflow/automation-engine",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "supabase start",
    "build": "echo 'Automation engine has no build step'",
    "deploy": "supabase functions deploy"
  }
}
